# ⚙️ Core Модуль

## 🎯 Назначение
Директория содержит основную бизнес-логику приложения для обработки аудио транскрипции.

## 📁 Структура
```
core/
├── __init__.py          # ✅ Инициализация core модуля
├── config_loader.py     # ✅ Загрузка конфигурации из config.yaml
├── logger.py            # ✅ Настройка системы логирования
├── transcriber.py       # 🚧 Интеграция с WhisperX
├── task_manager.py      # 🚧 Управление очередью задач
├── file_manager.py      # 🚧 Работа с файлами в shared директории
└── queue_processor.py   # 🚧 Обработчик приоритетной очереди
```

## 🔧 Основные компоненты

### ✅ Реализованные
- **config_loader.py** - Загрузка настроек из YAML
- **logger.py** - Система логирования с уровнями

### 🚧 К реализации
- **transcriber.py** - Запуск WhisperX процессов
- **task_manager.py** - Приоритетная очередь (DELETE > API > AUTO-SCAN)
- **file_manager.py** - Управление .in_progress/.result файлами
- **queue_processor.py** - Фоновая обработка задач

## 🔗 Связанные компоненты
- **API слой:** `../api/` - HTTP интерфейс
- **Конфигурация:** `../config.yaml` - настройки системы
- **Статусные файлы:** `../shared/` - .in_progress/.result
- **Модели:** `../models/` - кэш WhisperX моделей

## 🚀 Использование
```python
from core.config_loader import load_config
from core.logger import setup_logger
from core.transcriber import WhisperXProcessor
from core.task_manager import TaskQueue

# Основная логика приложения
```

## 📋 Ключевые функции (к реализации)
- **Обработка WhisperX** - subprocess управление
- **Приоритетная очередь** - DELETE(0) > API(1) > AUTO-SCAN(2)
- **Статусные файлы** - создание/обновление .in_progress/.result
- **Retry логика** - до 3 попыток с экспоненциальной задержкой
- **File polling** - проверка новых файлов в shared

См. [План разработки](../docs/development_plan.md) для детального roadmap.
