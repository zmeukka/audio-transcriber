# 🏗️ Структура проекта Audio Transcriber

## 📁 Обзор архитектуры

Audio Transcriber построен на модульной архитектуре с четким разделением ответственности между компонентами.

```
audio-transcriber/
├── 📁 api/                    # REST API интерфейс
├── 📁 core/                   # Основная бизнес-логика
├── 📁 docs/                   # Документация проекта
├── 📁 models/                 # Модели WhisperX
├── 📁 shared/                 # Общие файлы (входные/выходные)
├── 📁 specs/                  # Технические спецификации
├── 📁 tests/                  # Тесты
├── 📁 temp/                   # Временные файлы
├── 📄 app.py                  # Главный файл приложения
├── 📄 config.yaml             # Конфигурация
├── 📄 docker-compose.yml      # Docker Compose
├── 📄 Dockerfile              # Docker образ
├── 📄 requirements.txt        # Python зависимости
├── 📄 README.md               # Основная документация
├── 📄 LICENSE                 # Лицензия
├── 📄 DOCKER_SETUP.md         # Инструкции по Docker
├── 📄 get-docker.sh           # Скрипт установки Docker
├── 📄 setup_venv.ps1          # PowerShell setup скрипт
└── 📄 setup_venv.sh           # Bash setup скрипт
```

## 🔧 Основные компоненты

### 📡 API Layer (`api/`)
Слой REST API для взаимодействия с клиентами.

```
api/
├── __init__.py              # Инициализация модуля
├── models.py                # Pydantic модели данных
├── routes.py                # REST endpoints
└── README.md                # Документация API модуля
```

**Ответственность:**
- Обработка HTTP запросов
- Валидация входных данных
- Формирование ответов
- Обработка ошибок API

**Ключевые файлы:**
- `models.py` - Схемы данных (TranscribeRequest, TranscribeResponse, etc.)
- `routes.py` - Endpoints (/transcribe, /status, /result, /health)

### 🏠 Core Layer (`core/`)
Основная бизнес-логика приложения.

```
core/
├── __init__.py              # Инициализация модуля
├── config_loader.py         # Загрузка конфигурации
├── file_manager.py          # Управление файлами
├── logger.py                # Система логирования
├── task_manager.py          # Менеджер задач и очереди
├── transcriber.py           # Интеграция с WhisperX
└── README.md                # Документация Core модуля
```

**Компоненты:**

#### 📝 ConfigLoader (`config_loader.py`)
- Загрузка и валидация `config.yaml`
- Работа с переменными окружения
- Предоставление настроек другим модулям

#### 📂 FileManager (`file_manager.py`)
- Мониторинг shared директории
- Создание/чтение статусных файлов (.in_progress, .result)
- Автосканирование новых аудиофайлов
- Очистка временных файлов

#### 📋 TaskManager (`task_manager.py`)
- Система приоритетной очереди
- Управление жизненным циклом задач
- Обработка retry логики
- Мониторинг прогресса обработки

#### 🤖 Transcriber (`transcriber.py`)
- Интеграция с WhisperX через subprocess
- Обработка аудиофайлов
- Парсинг результатов транскрипции
- Управление моделями

#### 📊 Logger (`logger.py`)
- Централизованное логирование в stdout/stderr для Docker
- Конфигурация уровней логов
- Цветное форматирование для консоли

## 📊 Потоки данных

### 1. API Request Flow
```
Client Request → API Routes → Task Manager → Processing Queue
     ↓
Response ← API Routes ← Task Status ← File Manager
```

### 2. Background Processing Flow
```
Task Queue → Task Manager → Transcriber → WhisperX
     ↓
File Manager ← Result ← Transcriber ← WhisperX Output
```

### 3. File Monitoring Flow
```
Shared Directory → File Monitor → New Files → Task Manager → Queue
```

## 🗂️ Директории данных

### 📁 shared/ - Рабочая директория
Центральная директория для всех файлов обработки.

```
shared/
├── audio1.mp3              # Исходные аудиофайлы
├── audio1.in_progress      # Файлы в обработке
├── audio1.result           # Готовые результаты
├── interview.wav           # 
├── interview.result        # 
└── sample.mp3              # Sample файлы (исключаются из автосканирования)
```

**Типы файлов:**
- **Аудиофайлы:** `.mp3`, `.wav`, `.flac`, `.m4a`, `.ogg`, `.wma`
- **Статусные файлы:** `.in_progress` (JSON формат)
- **Результаты:** `.result` (JSON формат)

### 📁 models/ - Модели WhisperX
Кэш загруженных моделей машинного обучения.

```
models/
├── init_models.py          # Скрипт инициализации моделей
├── init.sh                 # Bash скрипт загрузки
├── init.ps1                # PowerShell скрипт загрузки
├── faster-whisper-tiny/    # Модель tiny (39 MB)
├── faster-whisper-base/    # Модель base (74 MB)
├── faster-whisper-small/   # Модель small (244 MB)
└── README.md               # Документация моделей
```


### 📁 temp/ - Временные файлы
Временные файлы WhisperX и промежуточные результаты.

```
temp/
├── whisperx_temp_abc123/   # Временные директории WhisperX
└── processing_cache/       # Кэш обработки
```

## 📚 Документация (`docs/`)

### Структура документации
```
docs/
├── ai_agent_methodology.md # 🤖 Методология работы с AI агентами
├── ai_trigger_quickref.md  # 🔥 Памятка по триггеру [ai]
├── api.md                  # 📡 REST API документация
├── configuration.md        # ⚙️ Настройки и конфигурация
├── conflicts_analysis.md   # 🔍 Анализ несостыковок
├── data_structures.md      # 📊 Структуры данных
├── deployment.md           # 🚀 Развертывание и деплой
├── development_plan.md     # 🗓️ План разработки
├── file_processing_logic.md # 🔄 Логика обработки файлов
├── project_structure.md    # 🏗️ Архитектура проекта
├── status_files.md         # 📋 Статусные файлы
└── user_guide.md           # 👥 Руководство пользователя
```

### Целевая аудитория документов
- `user_guide.md` - конечные пользователи
- `api.md` - разработчики интеграций
- `ai_agent_methodology.md` - AI агенты и разработчики (полная методология)
- `ai_trigger_quickref.md` - AI агенты (быстрая памятка по триггеру [ai])
- `deployment.md` - DevOps инженеры
- `development_plan.md` - программисты проекта
- остальные - техническая документация

## 🧪 Тестирование (`tests/`)

### Структура тестов
```
tests/
├── __init__.py
├── test_api.py             # Тесты REST API
├── test_core.py            # Тесты основной логики
├── test_integration.py     # Интеграционные тесты
├── test_file_manager.py    # Тесты работы с файлами
├── test_task_manager.py    # Тесты менеджера задач
└── README.md               # Документация тестов
```

### Типы тестов
- **Unit тесты** - отдельные функции и классы
- **Integration тесты** - взаимодействие компонентов
- **API тесты** - HTTP endpoints
- **E2E тесты** - полный workflow

## ⚙️ Конфигурация

### Основные конфигурационные файлы
```
├── config.yaml             # Основная конфигурация
├── docker-compose.yml      # Docker Compose setup
├── Dockerfile              # Docker образ
├── requirements.txt        # Python зависимости
├── .dockerignore          # Исключения для Docker
└── .gitignore             # Исключения для Git
```

### Переменные окружения
Приоритет настроек:
1. **Environment variables** (высший)
2. **config.yaml** 
3. **Defaults in code** (низший)

## 🔄 Жизненный цикл данных

### 1. Инициализация
```
app.py → ConfigLoader → Core modules → API setup → Background tasks
```

### 2. Обработка запроса
```
HTTP Request → API validation → Task creation → Queue → Processing → Response
```

### 3. Фоновая обработка
```
Queue polling → Task execution → WhisperX → Result saving → Status update
```

### 4. Завершение
```
Graceful shutdown → Task completion → File cleanup → Process termination
```

## 🔧 Модульность и расширяемость

### Принципы архитектуры
- **Separation of Concerns** - четкое разделение ответственности
- **Dependency Injection** - слабая связанность компонентов
- **Configuration driven** - настройка через конфигурацию
- **Async/await** - асинхронная обработка

### Точки расширения
- **Новые форматы аудио** - расширение FileManager
- **Дополнительные движки** - новые Transcriber implementations
- **Другие API endpoints** - добавление в routes.py
- **Система уведомлений** - hooks в TaskManager

### Интерфейсы для расширения
```python
# Абстрактный transcriber
class BaseTranscriber:
    async def transcribe_file(self, filename: str) -> dict
    async def get_available_models(self) -> list
    async def check_availability(self) -> bool

# Абстрактный file handler
class BaseFileHandler:
    async def handle_file(self, filepath: str) -> bool
    async def get_supported_extensions(self) -> list
```

## 📦 Зависимости

### Основные Python пакеты
```
fastapi              # Web framework
uvicorn             # ASGI server
pydantic            # Data validation
asyncio             # Async programming
pathlib             # Path handling
watchdog            # File monitoring
psutil              # System info
```

### Системные зависимости
```
ffmpeg              # Audio processing
curl                # Health checks
git                 # Version control
whisperx            # Transcription engine
```

## 🐳 Docker архитектура

### Multi-stage build
```dockerfile
# Stage 1: Base image with system dependencies
FROM python:3.11-slim as base

# Stage 2: Development environment
FROM base as development

# Stage 3: Production image
FROM base as production
```

### Volume mappings
```yaml
volumes:
  - ./shared:/app/shared           # Рабочие файлы
  - ./whisper_cache:/app/models    # Кэш моделей
  - ./config.yaml:/app/config.yaml # Конфигурация
```

---

**Дата обновления:** 3 ноября 2025  
**Версия:** 1.0
