# 📁 Руководство пользователя Audio Transcriber

Единая shared директория содержит все аудиофайлы и результаты их обработки через Docker API сервис.

## 📋 Поддерживаемые аудиоформаты

- `.mp3` - MP3 аудиофайлы
- `.wav` - WAV аудиофайлы
- `.m4a` - M4A аудиофайлы
- `.ogg` - OGG аудиофайлы
- `.flac` - FLAC аудиофайлы
- `.oga` - OGA аудиофайлы

## 📁 Структура файлов в shared директории

### 🎵 Аудиофайлы
- `audio.mp3` - исходный аудиофайл для обработки

### 📊 Статусные файлы
- `audio.in_progress` - файл обработки в процессе (содержит попытки и флаги)
- `audio.result` - файл с результатами транскрипции

### 🔄 Определение статуса обработки
- **Не обработан:** есть только `audio.mp3`
- **В процессе:** есть `audio.mp3` + `audio.in_progress`
- **Завершен:** есть `audio.mp3` + `audio.result`

## 🎵 Использование через API

### 📤 Запрос с настройками по умолчанию
Минимальный запрос - указывается только имя файла, остальные параметры берутся из конфигурации:
```bash
curl -X POST http://localhost:8000/transcribe \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "audio.mp3"
  }'
```

### 📤 Запрос с кастомными настройками
Полный запрос с переопределением параметров по умолчанию:
```bash
curl -X POST http://localhost:8000/transcribe \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "audio.mp3",
    "language": "en",
    "model_size": "large",
    "temperature": 0.0,
    "compute_type": "float16"
  }'
```

### 🗑️ Удаление файлов
Удаление аудиофайла и всех связанных файлов:
```bash
curl -X DELETE http://localhost:8000/transcribe \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "audio.mp3"
  }'
```

### 🗑️ Удаление нескольких файлов
Массовое удаление файлов:
```bash
curl -X DELETE http://localhost:8000/transcribe \
  -H "Content-Type: application/json" \
  -d '{
    "filenames": ["audio1.mp3", "audio2.wav", "audio3.m4a"]
  }'
```

## 📝 Требования к файлам

- **Максимальный размер файла:** 100MB (настраивается)
- **Кодировка файла:** Любая стандартная аудиокодировка
- **Именование файлов:** Любое валидное имя (избегайте спецсимволов)
- **Качество аудио:** Высокое качество = лучшие результаты транскрипции

## ⚙️ Поведение асинхронной обработки

Приложение работает в **двух состояниях**:

### 🔄 Состояние ожидания (нет активной обработки)
- **Сканирование директории:** Автоматическое сканирование shared директории на наличие новых файлов
- **Последовательная обработка:** Обработка всех найденных файлов по одному
- **Режим сна:** Процесс засыпает когда нет новых файлов
- **Автопробуждение:** Возобновляется при обнаружении новых файлов

### 🚀 Состояние активной обработки
- **Приоритетная обработка:** Новые API запросы имеют приоритет
- **Политика прерывания:** Обработка текущего файла прерывается если поступил новый запрос на другой файл
- **Переключение контекста:** Немедленно переключается на обработку нового запрошенного файла
- **Управление очередью:** Прерванные файлы возвращаются в очередь для последующей обработки

### 📋 Приоритет обработки
1. **API запросы** (высший приоритет - прерывает текущую обработку)
2. **Файлы из сканирования директории** (обрабатываются последовательно в режиме ожидания)
3. **Прерванные файлы** (возобновляются при отсутствии задач с высшим приоритетом)

### 🔄 Обработка дублирующихся запросов

При поступлении API запроса на файл, который уже обрабатывается:

#### ✅ Если настройки совпадают:
- **Ожидание завершения:** Запрос присоединяется к текущей обработке
- **Общий результат:** Все запросы получают одинаковый результат
- **Оптимизация ресурсов:** Файл обрабатывается только один раз

#### 🔄 Если настройки различаются:
- **Прерывание обработки:** Текущая обработка немедленно прекращается
- **Новая обработка:** Файл обрабатывается заново с новыми настройками
- **Приоритет запроса:** Настройки из API запроса имеют приоритет над настройками по умолчанию
- **Перезапуск с нуля:** Обработка начинается с начала файла

#### 🎯 Сравниваемые параметры:
- `model_size` - размер модели
- `language` - язык транскрипции
- `temperature` - температура модели
- `compute_type` - тип вычислений
- `device` - устройство для обработки

### 🔄 Поведение при DELETE запросе:
- **Прекращение обработки:** Если файл обрабатывается - обработка останавливается
- **Удаление всех связанных файлов:** Удаляются все `filename.*` (исходный файл, статусные файлы, результаты)
- **Очистка очереди:** Файл удаляется из очереди обработки

## 📝 Структура файла `.in_progress`

Содержит JSON с информацией о процессе обработки:
```json
{
  "attempts": [
    {
      "attempt_number": 1,
      "start_time": "2024-01-01T10:00:00Z",
      "settings": {
        "model_size": "base",
        "language": "ru"
      },
      "continue_processing": true
    }
  ],
  "current_attempt": 1,
  "should_continue": true
}
```

## 🤖 Конфигурация Whisper

### 🔧 Параметры по умолчанию
Если параметры не указаны в API запросе, используются значения по умолчанию:
- **model_size:** `base` - хороший баланс скорости и точности
- **language:** `ru` - русский язык
- **temperature:** `0.1` - для лучшей точности
- **compute_type:** `int8` - оптимальная производительность
- **device:** `cpu` - процессорные вычисления
- **debug:** `false` - отключен режим отладки

### 📊 Доступные размеры моделей
- **tiny** - ~39MB, самая быстрая, базовая точность
- **base** - ~74MB, хороший баланс скорости и точности (по умолчанию)
- **small** - ~244MB, улучшенная точность
- **medium** - ~769MB, высокая точность
- **large** - ~1550MB, максимальная точность

### 🌍 Поддерживаемые языки
- **ru** - Русский (по умолчанию)
- **en** - Английский
- **es** - Испанский
- **fr** - Французский
- **de** - Немецкий
- И другие языки, поддерживаемые Whisper

### ⚡ Настройки производительности
- **compute_type:** `float32` (высокая точность), `float16` (баланс), `int8` (быстро, по умолчанию)
- **device:** `cpu` (по умолчанию), `cuda` (для GPU)
- **temperature:** `0.0-1.0` (0.1 по умолчанию для лучшей детерминированности)

## 🔧 Монтирование Docker томов

```yaml
volumes:
  - ./shared:/app/shared
  - ./whisper_cache:/root/.cache/whisper
```

API сервис использует конфигурацию из `config.yaml` для параметров по умолчанию и кеширования моделей.

## 📊 Оптимальные настройки для качества

- **Битрейт:** 128kbps или выше
- **Частота дискретизации:** 16kHz или выше
- **Каналы:** Моно или стерео
- **Фоновый шум:** Минимальный для лучших результатов
- **Четкость речи:** Четкое произношение улучшает точность
