# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Audio Transcriber - Дополнительные спецификации

> **Примечание:** Основная документация находится в папке `docs/`. Данный файл содержит только уникальные технические детали, не покрытые в документации.

### 📌 Цель проекта
Разработать Docker-контейнер для автоматической транскрипции аудиофайлов через API с использованием WhisperX.

### 🎯 Ключевые ограничения обработки

**Sample файлы:**
- Файлы с именем содержащим "sample" (регистронезависимо) исключаются из автосканирования
- Sample файлы обрабатываются только при явном API запросе  
- После добавления в очередь через API, sample файлы обрабатываются как обычные

**Кодировка файлов:** UTF-8 для всех текстовых файлов (.result, .in_progress)

### 🔧 Технические требования (уникальные детали)

**Язык комментариев в коде:** Английский (все комментарии должны быть написаны на английском языке)

**Base Docker image:** python:3.11-slim или ubuntu:22.04

**Архитектура компонентов:** См. [project_structure.md](../docs/project_structure.md)

### 📡 API спецификация

> **Полная API документация:** См. [api.md](../docs/api.md)

**Уникальные детали:**

**Debug режим приоритет:**
1. **Параметр debug в API запросе** имеет высший приоритет
2. **Настройка debug в config.yaml** используется только если параметр не передан в запросе
3. **По умолчанию debug = false** если не указано ни в запросе, ни в конфигурации

### 📁 Конфигурация

> **Полная конфигурация:** См. [configuration.md](../docs/configuration.md)

### 🔄 Логика обработки файлов

> **Детальная логика:** См. [file_processing_logic.md](../docs/file_processing_logic.md)

**Уникальные алгоритмы:**

- **Удаление аудио файла после успешной обработки** → См. [file_processing_logic.md](../docs/file_processing_logic.md#audio-cleanup)
- **Автоочистка старых результатов** → См. [file_processing_logic.md](../docs/file_processing_logic.md#auto-cleanup)
- **Обработка конфликтных состояний файлов** → См. [file_processing_logic.md](../docs/file_processing_logic.md#conflict-resolution)

### ⏱️ Временные параметры

- **Проверка файла:** 5 попыток × 1 секунда = 5 секунд максимум
- **Polling обработки:** каждые 5 секунд при ожидании
- **Timeout обработки:** 180 секунд (3 минуты) максимум
- **Retry обработки:** до 3 попыток с экспоненциальной задержкой

### 🎯 Система приоритетов

**Приоритеты обработки:**
1. **DELETE запросы** - выполняются немедленно, минуя очередь
2. **GET/POST запросы через API** - высший приоритет в очереди (priority = 1)
3. **Файлы из автосканирования shared директории** - низший приоритет (priority = 2)

**Структура задачи в очереди:** См. [data_structures.md](../docs/data_structures.md#processing-task)

### 🐳 Docker развертывание

> **Полная документация по развертыванию:** См. [deployment.md](../docs/deployment.md)

**Уникальные детали:**

#### Environment Variables
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR)
- `WHISPERX_MODEL_SIZE` - размер модели по умолчанию
- `WHISPERX_LANGUAGE` - язык по умолчанию
- `WHISPERX_DEVICE` - устройство по умолчанию (cpu/cuda)

### 🐛 Debug режим и обработка ошибок

> **Детальная документация:** См. [api.md](../docs/api.md#debug-mode) и [configuration.md](../docs/configuration.md#debug-settings)

**Debug режим в config.yaml:**
- При включении debug режима API возвращает детальную информацию об ошибках
- Включает содержимое .in_progress файлов в ответах при ошибках
- Приоритет настроек: API запрос > config.yaml > по умолчанию false

**Обработка ошибок:**
- **Стандартный режим:** Простые сообщения об ошибках
- **Debug режим:** Полная диагностическая информация включая:
  - Содержимое .in_progress файлов
  - История всех попыток обработки
  - Временная шкала обработки
  - Детали ошибок WhisperX

**Примеры API ответов:** См. [api.md](../docs/api.md#error-responses)

---

**Версия документа:** 5.0 (Без примеров кода)  
**Дата создания:** 29 октября 2025  
**Дата обновления:** 3 ноября 2025  
**Статус:** Упрощено - удалены все примеры кода, оставлены только ссылки на docs/

> **Примечание:** Основная техническая документация находится в папке `docs/`. Данный файл содержит только уникальные детали реализации, не покрытые в основной документации.
